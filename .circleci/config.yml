version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.9
    working_directory: ~/ci-test
    steps:
      - checkout
      - run:
          name: install-dependencies
          command: yarn install
      - save_cache:
          paths:
            - node_modules
          key: v1-dependencies-{{ checksum "node_modules" }}
  compile:
    docker:
      - image: circleci/node:10.9
    steps:
      - checkout
      - restore_cache:
          key: v1-dependencies-{{ checksum "node_modules" }}
      - run:
          name: app-build
          command: yarn run build
  deploy:
    docker:
      - image: circleci/node:10.9
    steps:
      - checkout
      - restore_cache:
          key: v1-dependencies-{{ checksum "node_modules" }}
      - run:
          name: app-deploy
          command: yarn run deploy
workflows:
  version: 2
  compile_and_test:
    jobs:
      - build
      - compile:
          requires:
            - build
      - deploy:
          requires:
            - build
            - compile
          filters:
            branches:
              only: master